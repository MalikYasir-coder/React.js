version: '3.8'

services:
  backend:
    build: .
    container_name: fake-news-backend
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - DEBUG=True
      - PORT=5000
    volumes:
      - .:/app
    command: flask run --host=0.0.0.0
